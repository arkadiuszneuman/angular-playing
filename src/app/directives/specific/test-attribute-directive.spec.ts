import { TestAttributeDirective } from './test-attribute-directive';

describe('TestAttributeDirective', () => {
  it('should create an instance', () => {
    const directive = new TestAttributeDirective();
    expect(directive).toBeTruthy();
  });
});
