import { TestValidationDirective } from './test-validation-directive';

describe('TestValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new TestValidationDirective();
    expect(directive).toBeTruthy();
  });
});
